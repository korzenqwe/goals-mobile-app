export const REMINDERS_ENABLED = false

export type RemindersPlaceholder = {
  enabled: false
  plannedFor: 'post-mvp'
}

export const remindersPlaceholder: RemindersPlaceholder = {
  enabled: false,
  plannedFor: 'post-mvp',
}
